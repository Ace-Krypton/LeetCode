#pragma once

#include <vector>
#include <iostream>
#include <algorithm>
#include <gtest/gtest.h>

struct ListNode {
    int32_t value;
    ListNode *next;
};

class Solution {
public:
    static auto pair_sum(ListNode *head) -> int32_t {

    }

    static auto insert(ListNode **head_ref)
};
